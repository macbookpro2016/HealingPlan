<view class="container">
  <!-- 顶部区域：小太阳和温馨提示 -->
  <view class="top-section">
    <text class="comfort-text">别伤心，这里是你的港湾</text>
    <view class="sun-container">
      <view class="sun-shape">
        <text class="sun-text">{{healingDays}}</text>
      </view>
    </view>
  </view>

  <!-- 开发者寄语（可折叠） -->
  <view class="dev-msg-section {{devMsgExpanded ? 'expanded' : ''}}">
    <view class="dev-header" bindtap="toggleDevMsg">
      <text class="dev-title">开发者寄语</text>
      <image class="expand-icon" src="{{devMsgExpanded ? '/images/arrow-up.png' : '/images/arrow-down.png'}}"></image>
    </view>
    <view class="dev-content">
      <text>每一次记录都是治愈的一步</text>
      <text>在这里，你可以放下所有烦恼</text>
      <text>让我们一起见证你的成长</text>
      <text>愿每一天都比昨天更美好</text>
    </view>
  </view>

  <!-- 日记和打卡记录 -->
  <view class="record-section">
    <text class="record-title">我的治愈记录</text>
    <view class="record-list" wx:if="{{recordList.length > 0}}">
      <view class="record-item {{item.type === 'check_in' ? 'check-in-item' : 'diary-item'}}" 
            wx:for="{{recordList}}" wx:key="id">
        <!-- 交换顺序，内容在前，日期在后 -->
        <text class="record-content">{{item.content}}</text>
        <text class="record-date">{{item.date}}</text>
      </view>
    </view>
    <view class="empty-tip" wx:else>
      <text>暂无记录，快去添加第一条治愈记录吧～</text>
    </view>
  </view>
</view>